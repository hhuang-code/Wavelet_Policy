_target_: models.latent_generators.waveletnet.WaveletNet

discrete_input: false
input_dim: ${encoder.output_dim}

vocab_size: ???  # TBD by the discretization model.

# Architecture details
n_layer: ${n_layer}
n_head: 6
n_embd: 120

kernel_height: 1
kernel_width: 3

converter: ${converter}
scaler: ${scaler}

block_size: ${window_size}  # Length of history/context
predict_offsets: True
offset_loss_scale: 1000.0
focal_loss_gamma: 2.0
action_dim: ${env.action_dim}

detail_loss_weight: ${detail_loss_weight}
smooth_loss_weight: ${smooth_loss_weight}

